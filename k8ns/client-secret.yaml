apiVersion: v1
kind: Secret
metadata:
  name: chatverse-client-secret
type: Opaque
stringData:
  # Base API URL used by RTK Query; client currently hardcodes localhost.
  # Adjust client code to read from this at build/runtime if needed.
  VITE_API_BASE_URL: "http://chatverse-server:4000/api/v1"
  VITE_SOCKET_URL: "http://chatverse-server:4000"

